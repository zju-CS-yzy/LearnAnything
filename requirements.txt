# LearnAnything 核心依赖
# 注意：chromadb 已从代码中移除（替换为 SQLite + numpy），保留此行仅为兼容性注释
# chromadb>=0.4.0

# 后端框架
fastapi>=0.136.0
uvicorn>=0.49.0
pydantic>=2.13.0
pydantic-settings>=2.14.0
python-multipart>=0.0.30
jinja2>=3.1.0
markupsafe>=3.0.0

# ASGI/HTTP 依赖链（fastapi 的间接依赖）
anyio>=4.0.0
starlette>=1.3.0
sniffio>=1.3.0
idna>=3.0.0
h11>=0.14.0
click>=8.0.0
colorama>=0.4.0
exceptiongroup>=1.0.0

# 数据/配置处理
requests>=2.31.0
numpy>=1.24.0
PyYAML>=6.0.0
python-dateutil>=2.9.0
packaging>=24.0
jsonschema>=4.26.0
certifi>=2024.0.0

# 文档处理
jieba>=0.42.1
rank_bm25>=0.2.2
PyMuPDF>=1.23.0
Pillow>=10.0.0

# 图数据库
kuzu>=0.11.0

# 桌面应用
PyQt5>=5.15.0

# OCR/公式识别（可选，体积大，当前环境未安装）
# paddlepaddle>=2.5.0
# paddleocr>=2.7.0
# pix2tex>=0.1.2
# 注意：以上三个包体积巨大（>500MB），且包含原生扩展。
# 当前代码已实现降级处理（OCR 失败时跳过，公式识别失败时跳过）。
# 如需启用，请手动安装。
