# 工作记录 — 2026-07-21

## 时间：01:32 - 02:00

### 1. LA-027: 前端重建验证 — 发现技术→技术非法连接

**开发者反馈**（engineering 范式重建 RAG 学科后）：
- ✅ type 分布正确（只有 requirement / technology）
- ✅ 边的方向正确（没有环）
- ❌ **出现技术→技术的连接**，中间没有 gap 节点
- ❌ **没有 gap 标记**（gap 检测未实现）

**修复内容**：

| 文件 | 修改 |
|:---|:---|
| `core/semantic_linker.py` | 新增 `_load_paradigms_from_yaml()`：从 paradigms.yaml 读取 relation_map |
| `core/semantic_linker.py` | 新增 `_build_relation_validator()` / `_is_valid_relation()`：关系合法性校验函数 |
| `core/semantic_linker.py` | `link_all()`：合并边后增加 relation_map 校验过滤，非法连接被拒绝并打印日志 |
| `core/semantic_linker.py` | 新增 `_get_concept_type()`：根据 canonical_id 查询 concept_type（支持缓存） |
| `core/semantic_linker.py` | `_load_canonical_concepts()` / `_load_from_kuzudb()`：初始化并填充 `_concept_type_cache` |

**测试验证**：
- `scripts/test_relation_map_validation.py` — 4 项测试全部通过
  - `requirement --IMPLEMENTS--> technology`: 合法 ✓
  - `technology --DEPEND_ON--> requirement`: 合法 ✓
  - `technology --DEPEND_ON--> technology`: 非法（已拒绝）✓
  - `requirement --DEPEND_ON--> technology`: 非法（relation_type 不匹配）✓

**清理旧边**：
需要先删除已有非法语义连接边，再重新运行语义链接：
```cypher
MATCH ()-[r:SOLUTION|DEPENDS_ON]->() DELETE r
```

---

## 时间：01:52

### 2. TutorAgent 持久会话测试确认

**测试结果**：✅ 持久会话已实现
- 退出后端并重启后，正确读取历史会话
- 历史会话正确注入上下文

**新发现的3个问题**：

| 编号 | 问题 | 优先级 |
|:---|:---|:---|
| LA-047 | 缺少引用能力：LLM 回答未标注知识来源（章节/页码） | P0 |
| LA-048 | Markdown 渲染失效：`####`/`###` 标题未正确渲染 | P0 |
| LA-049 | 图片媒体引用失败：前端破碎图标 + 后端 Cypher 语法错误/404 | P0 |

**修复方案概要**：

- **LA-047（引用能力）**：RAG 检索时记录来源 chunk 的 heading_path + page_number，Prompt 要求 LLM 添加引用列表，前端渲染为可点击链接
- **LA-048（Markdown 渲染）**：检查前端 Markdown 解析器配置（marked.js/markdown-it），排查 CSS 覆盖问题，确认原始文本未转义
- **LA-049（图片引用）**：修复 `collect_media_resources()` 的 Cypher 查询构造（chunk_id 转义），检查 `/api/media/` 路由对 `image_pseudo` 的处理，确认 MinerU 图片保存到 static 目录

**Gap 检测**：
- 遗留问题 LA-046：gap 检测逻辑完全未实现，需专门开发

---

## 明日计划

1. **LA-046**: Gap 检测逻辑实现（`ideal_chain` + `relation_map` + 降级策略）
2. **LA-047/LA-048/LA-049**: 解决智能问答中的3个遗留问题

---
*记录时间: 2026-07-21 02:00*
