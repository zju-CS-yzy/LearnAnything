# 2026-07-23 工作记录

## 时间段：00:00-00:38（跨日期工作）

---

## 一、今日（2026-07-22）完成事项汇总

### P0: LA-027 - YAML 范式配置与后端代码同步 ✅

**问题**：RAG 学科重建时所有语义连接边被 relation_map 校验拒绝，0 条边写入。

**根因**：`semantic_linker.py` 的 `PARADIGM_LEVELS` 硬编码与 YAML v2.0 完全脱节（4层 vs 2层，SOLUTION/DEPENDS_ON vs IMPLEMENTS/DEPEND_ON）。

**修复**（`core/semantic_linker.py`）：
- 新增 `_build_paradigm_config()`：优先从 `paradigms.yaml` 加载配置
- `link_all()` 使用 YAML 配置替代硬编码
- `_determine_cross_level_relation()` 从 YAML 动态推导关系类型
- 调整执行顺序：gap 检测 → 虚拟节点 → relation_map 校验

**修复**（`core/graph_store.py`）：
- 新增 `_ensure_paradigm_rel_tables()`：从 YAML 动态创建关系表
- 解决 `Table IMPLEMENTS does not exist` 错误

**验证**：RAG 学科重建成功，写入 310 条语义连接边 + 125 个虚拟节点。

### P0: LA-046 - Gap 检测 + 虚拟节点 ✅

**实现**（`core/semantic_linker.py`）：
- `_calculate_gap()`: 支持循环/非循环范式的 gap 计算
- `_process_gaps_and_virtual_nodes()`: 检测 gap → 创建虚拟节点 → 替换为合法边
- `_write_virtual_nodes()`: 虚拟节点写入 KùzuDB（`is_virtual = true`）
- 4 处自环过滤（策略1-3、策略4、embedding+LLM、gap 处理）

**前端样式修复**（多轮调试最终方案）：
- `GraphView.vue`: 虚拟节点在 `cy.add()` 时直接传入 `style` 属性（绕过 CSS 选择器）
- 设置 `width: 24, height: 24, shape: ellipse, border: 2px dashed orange`
- 普通节点：大卡片（圆角矩形），类型颜色填充（红=需求，蓝=技术）
- 虚拟节点：24px 橙色虚线小圆

**状态**：✅ **已测试通过**（2026-07-23 00:30）

### P1: LA-052 - 范式配置动态获取方案（部分完成）

**设计文档**：`docs/design-paradigm-dynamic-config.md`

**已完成**：
- 后端 `/api/knowledge-graph/{subject}/paradigm` API（`app/backend_api.py`）
- YAML `styles` 字段（`config/paradigms.yaml`）
- 前端 `ParadigmConfig.js` 模块（加载/缓存/辅助函数）
- `GraphLayout.js` 使用 `isSemanticEdge()` 替代硬编码

**待完成**：
- `GraphView.vue` 动态图例渲染（当前硬编码）
- `GraphStyles.js` 动态样式生成（当前使用静态 CSS）
- 学科切换时范式配置动态加载

### P2: VLM 超时调整 ✅

`core/vlm_client.py`: `timeout=120` → `timeout=300`

### P2: LLM SSL 错误重试 ✅

`core/llm_client.py`: 捕获 `SSLError`/`ConnectionError`，3s→9s→27s 指数退避

### GitHub 仓库清理 ✅

- 使用 `git-filter-repo` 清理历史大文件（2.50 GiB → 137.66 MiB）
- 更新 `.gitignore` 排除运行时数据
- 推送至 https://github.com/zju-CS-yzy/LearnAnything

---

## 二、进行中 / 待验证

### LA-052: 范式配置动态获取（剩余工作）
- `GraphView.vue` 图例动态渲染
- `GraphStyles.js` 动态样式
- 学科切换自动加载范式配置

### 树深度问题（2层 vs 预期3-5层）
- 当前：最深树只有 2 层（requirement ↔ technology）
- 预期：递归结构 requirement → technology → requirement → technology
- 可能原因：
  1. 输入知识库材料本身的 parent_hint 质量
  2. SemanticLinker 的 embedding+LLM 阶段过滤过多
  3. 虚拟节点边写入后，后续连接未正确建立
- 需要：重建后检查后端虚拟节点边日志，确认 virtual→child 边是否正确写入

---

## 三、遗留问题清单更新

| 编号 | 优先级 | 问题 | 状态 | 说明 |
|:---|:---|:---|:---|:---|
| **LA-046** | P0 | Gap 检测 + 虚拟节点 | ✅ **已测试通过** | 虚拟节点样式正确显示，自环已过滤 |
| **LA-052** | P1 | 范式配置动态获取 | 🟡 **部分完成** | 设计文档+API+模块已完成，前端集成待完成 |
| **LA-044-#2** | P1 | Agent 个性化 Prompt | 🔴 未开始 | TutorAgent 根据 user_theta 调整讲解深度 |
| **LA-044-#3** | P1 | UserStateStore 同步 | 🔴 未开始 | 对话结束时 theta/薄弱点同步 |
| LA-051 | P2 | Git 双仓库架构 | 🔴 未开始 | 私有 Dev + 公开 Release |
| LA-050 | P2 | VLM API 调用不稳定 | 🔴 未开始 | HTTP 500/400 错误 |
| 树深度 | P1 | 概念树深度不足 | 🟡 **排查中** | 预期3-5层，实际2层 |

---

## 四、Git Commit 记录（2026-07-22）

1. `fix(LA-027/LA-046): SemanticLinker 优先从 paradigms.yaml 加载范式配置`
2. `fix(LA-027): KùzuDB schema 动态关系表 + LLM SSL 错误重试`
3. `chore: 清理运行时数据，更新 .gitignore`
4. `fix(LA-027): 前端支持新旧关系类型（IMPLEMENTS/DEPEND_ON）`
5. `fix(LA-052): is_virtual 布尔值类型兼容修复`
6. `fix(LA-052): Cytoscape 虚拟节点选择器使用值匹配`
7. `fix(LA-052): 移除 cy.style().fromJson() 避免样式优先级破坏`
8. `fix(LA-052): 虚拟节点样式移到概念节点样式之前`
9. `fix(LA-052/LA-046): 概念样式排除虚拟节点 + 自环边过滤`
10. `fix(LA-052): 将复合概念节点选择器拆分为独立选择器`
11. `fix(LA-052/LA-046): 前端虚拟节点直接设置style + 后端虚拟节点边调试日志`
12. `fix(LA-052): GraphLayout.js 使用 isSemanticEdge() + 范式配置设计文档`

---

## 五、明日（2026-07-23）工作计划

### P0: 验证 RAG 学科重建结果
1. 检查后端输出中虚拟节点边写入日志
2. 确认 `[搜索增强系统]` 是否正确连接到 `[缺失]需求层` 虚拟节点
3. 检查树深度是否达到 3-5 层

### P1: LA-052 完成前端集成
1. `GraphView.vue` 图例动态渲染（v-for 遍历 paradigmConfig.relations）
2. `GraphStyles.js` 动态样式生成（根据范式配置创建边样式）
3. 学科切换时自动重新加载范式配置

### P1: 排查树深度问题（如仍存在）
1. 检查 SemanticLinker 各阶段边数统计
2. 检查 embedding+LLM 阶段过滤比例
3. 考虑降低阈值或优化 LLM 提示词

### P2: LA-044-#2 Agent 个性化 Prompt
1. TutorAgent 根据 `user_theta` 调整讲解深度
2. QuizAgent 根据 `weak_areas + topic` 出题

---

*记录时间：2026-07-23 00:38*
