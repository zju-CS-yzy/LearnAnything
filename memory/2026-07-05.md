# 2026-07-05 — LearnAnything 设计文档 + 导入流程修复

## 完成工作

### 1. 创建设计文档 (`docs/DESIGN.md`)
- 项目简要说明、目标效果、知识图谱构建思想
- 数据模型（Chunk/Concept/边/学科元数据）
- 技术栈与架构（前端/后端/Agent/数据层）
- 各模块实现方式（7个核心模块）
- 数据流（导入/提取/查询）
- 前端架构（Vue3 + Cytoscape + 自定义布局算法）
- 扩展性设计（新学科/Agent/前端/后端）
- 测试策略（单元/集成/E2E/CI）
- 数据迁移（Schema/备份/恢复/导出）

### 2. 修复导入流程核心 bug
- `app/backend_api.py`: `import_file` 和 `import_text` 现在同时写入 **向量数据库 + KùzuDB**
  - 之前只写入 ChromaDB，导致新学科 graph DB 为空
- `core/subject_manager.py`: `record_import` 同时更新 `document_count` + `raw_files_count`
  - 之前只更新 document_count，导致导入统计显示 raw_files=0

### 3. 修复前端问题
- `GraphView.vue`: `'height': 'label'` → `'height': 60`（Cytoscape 废弃语法导致崩溃）
- 修复 chunk 类型查询：`chunk_type = 'child'` → `chunk_type <> 'parent'`
  - 数据库中 chunk 实际类型为 markdown（162个），child 仅 1 个

### 4. 布局算法调整
- `runLayout()`: 自定义树形布局（自底向上计算子树高度、父节点居中、复制共享子节点）
- `runConceptLayout()`: 概念节点布局（dagre LR + 共享子节点复制），保留为独立函数

## 遗留问题

### 🔴 LA-025: 前端图谱不显示（ai_llm 学科）
- ai_llm graph DB 为空（之前导入时未写入 KùzuDB）
- 代码已修复，但需要重新导入 3 个 PDF
- 自定义布局算法可能仍有 bug（ADJACENT_TO 链式结构）

### 🟡 LA-026: 前端布局与 7月2日效果差距
- 7月2日效果：节点 302 | 边 253，树形结构清晰
- 当前效果：待验证（可能节点重叠）
- 需要对比 7月2日代码（git 历史）或重新调参

## 明日计划
1. 重新导入 ai_llm PDF 验证修复效果
2. 检查浏览器控制台报错
3. 如需调整：对比 7月2日 layout 参数
