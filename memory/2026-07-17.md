# 2026-07-17 — LA-035-P27 前端图片显示问题排查与修复

## 今日工作

### 1. P27 排查：前端图片显示失败

**用户现象**：点击有 `图片×1` 标签的节点，NodeDetailPanel 不显示图片，Network 中无图片请求。

**排查链路**：
1. 读取前端代码：GraphView.vue / NodeDetailPanel.vue / GraphNodeTooltip.vue
   - `NodeDetailPanel` 图片 URL 构造逻辑正确：`/api/images/{subject}/{filename}`
   - `hasMedia` 条件：`media_refs?.length > 0`
2. 写诊断脚本（多次迭代，处理编码问题）
3. KùzuDB 数据检查：
   - `rag_v1`：45/756 概念节点有图片（6%）
   - `transformer_v1`：27/139 概念节点有图片（19%）
4. **用户截图分析**：节点 ID 为 `concept_canonical_b9dcd94454`（"文本生成"）
5. 该节点 `media_refs` 为空，fallback 从 `source_chunks` 获取也失败

**根因**：
- Chunk 节点 `media_refs` 存储了 Windows 绝对路径：`D:\MyCS\...`
- JSON 中反斜杠未转义 → `json.loads` 失败：`Invalid \escape`
- `_get_media_refs_from_chunks` fallback 解析失败 → 概念节点 `media_refs` 为空
- 前端 `hasMedia` 为 false → 不渲染 `img` → Network 无图片请求

### 2. 修复

**后端存储逻辑修复**（`core/graph_store.py`）：
1. 新增 `_sanitize_media_refs()` 方法：
   - 使用 `relative_path` 替代 `path`（避免 Windows 绝对路径）
   - 反斜杠替换为正斜杠
2. 在 3 个写入路径调用：
   - `add_chunk_nodes`（Chunk 节点写入）
   - `store_extracted_concepts`（提取概念写入）
   - `store_canonical_concepts`（去重概念写入）

**现有数据修复**：
- 脚本 `scripts/fix_media_refs.py`：遍历所有数据库，修复 Chunk + CanonicalConcept 的 media_refs
- 修复结果：
  - `rag_v1`：123 个概念节点新增 media_refs（45 → 168，+123）
  - `transformer_v1`：27 个概念节点新增 media_refs（27 → 54，+27）
- 节点"文本生成"（b9dcd94454）已修复：path 为 `rag_v1_images/tmpmevygvhq_mineru_2_2108b2b2.png`

### 3. Git Commit

```
a34e178 fix(storage): LA-035-P27 - sanitize media_refs paths before write
76ed9d8 docs: update LA-035-P27 status - backend storage fixed, pending frontend verification
```

### 4. 遗留问题更新

**P27**: 🟡 后端修复完成，待前端验证
- 验证步骤：刷新前端 → 点击"文本生成"节点 → 检查 NodeDetailPanel 显示图片 + Network 出现 `/api/images/rag/...` 请求

---

## 临时脚本（未提交到 git）

本次排查生成的诊断脚本（存于 `scripts/` 目录，未提交）：
- `diagnose_media_refs.py` — 数据库 media_refs 统计诊断
- `diagnose_node_image.py` — 特定节点诊断
- `check_node.py` / `check_chunk.py` / `check_b9d.py` / `search_node.py` — 节点搜索与验证
- `fix_media_refs.py` — 批量修复脚本
- `patch_graph_store.py` — 自动补丁脚本（插入 `_sanitize_media_refs`）

这些脚本可保留作为参考，但已标记为临时文件，不进入 git。

### 5. P30 排查：文档树卡死

**用户观察**：CanonicalConcept 树（756 节点）不卡死，但文档树（454 chunk 节点）卡死，说明不是数量问题。

**排查链路**：
1. 读取 `GraphView.vue` / `GraphLayout.js`：`runTreeLayout` 有 >200 节点保护机制
2. 写诊断脚本 `analyze_tree_structure.py` 分析数据库结构
3. **关键发现**：BELONGS_TO / ADJACENT_TO 结构完全混乱：
   - BELONGS_TO: 816 条 paragraph→paragraph（应为 ADJACENT_TO），82 条反向 paragraph→heading，102 条反向 image_pseudo→paragraph
   - ADJACENT_TO: 551 条 heading→paragraph（层级关系混入同级顺序），113 条 heading→heading
   - document 节点无出边（5 个叶子），167 个 heading 无父节点
   - 根节点：167 heading + 21 paragraph（伪根）

**根因**：`build_belongs_to_relations` 按 heading_path 分组后，组内所有相邻 chunk 都建 BELONGS_TO（不区分类型）；`build_adjacent_relations` 按全局文档排序建边（跨 heading）；document 未连接 heading。

**修复**（后端数据重构）：
1. 删除旧边：DELETE 1527 BELONGS_TO + 2221 ADJACENT_TO（KuzuDB 支持 DELETE 关系）
2. 重建正确结构：
   - document→heading: 82（5 文档 → 82 顶层标题）
   - heading→heading: 101（数字编码前缀匹配 + `>` 路径分隔）
   - heading→paragraph: 356（heading_path 匹配）
   - heading→image_pseudo: 25
   - paragraph→paragraph: 215（同级顺序）
   - image_pseudo→image_pseudo: 1

**代码修复**（`core/graph_store.py`）：
- 重写 `build_belongs_to_relations`：加载所有 chunks，建立 heading_by_path / heading_by_code 映射，提取数字编码和 `>` 分隔层级
- 重写 `build_adjacent_relations`：按 (source, heading_path, type) 分组，仅同级 chunk 建边
- `build_has_parent_relations`：标记为已合并到 BELONGS_TO

**Git Commit**:
```
d839911 fix(graph): LA-035-P30 - rebuild document tree structure with correct hierarchy
```

### 6. 遗留问题更新
- **P27**: 🟡 后端修复完成，待前端验证（已提交 a34e178 / 76ed9d8）
- **P30**: 🟡 数据重构完成，待前端验证（已提交 d839911）
- 新增 P30 根因详细记录

### 7. 临时脚本（P30 未提交）
- `analyze_tree_structure.py` — 文档树结构分析
- `analyze_heading_codes.py` — heading 编码模式分析
- `fix_tree_structure.py` — 数据重构脚本（删除旧边 + 重建新边）
- `verify_tree_structure.py` — 验证重构后结构
- `patch_graph_store_tree.py` / `replace_belongs_to.py` — 代码补丁脚本
- `delete_edges.py` — 删除旧边脚本

---

## 回家验证步骤（P27 + P30）

### P27 验证：
1. 刷新浏览器，加载 rag_v1 学科
2. 找到节点 "文本生成"（底部应有 `图片×1` 标签）
3. 点击，检查 NodeDetailPanel 右侧是否显示图片
4. Network 面板是否出现 `/api/images/rag/...` 请求

### P30 验证：
1. 切换到文档树视图
2. 检查是否还卡死（应该能正常渲染）
3. 检查树结构：文档→标题→段落，层次清晰
4. 检查同级段落是否按顺序排列
