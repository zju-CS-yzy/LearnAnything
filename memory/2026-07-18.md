# 2026-07-18 — LearnAnything 开发日志

## 今日修复（验证通过）

### LA-040-P0-QUIZ: Agent 出题流程回退到旧方式 ✅
**从"完全回退"到"P0 完整跑通"，历经 10 轮修复。**

最终状态：出题成功走 P0 流程，从 "RAG" 种子扩展出 11 节点 10 边，返回 RAG 相关题目。

**修复链路**（按时间顺序）：
1. **2026-07-16**: P0-INT 设计落地 — Coordinator 集成 P0 模块、QuizAgent 使用 ContextAssembler、CoachAgent 使用 IRTEstimator、UserKnowledgeState SQLite 持久化、消息总线
2. **2026-07-17**: KuzuDB 文件锁定 → 全局 `_graph_store_cache` 共享 GraphStore
3. **2026-07-17**: API 端点绕过 Coordinator → `/api/quiz` 和 `/api/evaluate/start` 走 Coordinator + 共享 GraphStore
4. **2026-07-17**: QuizAgent 未订阅消息总线 → `_subscribe_to_message_bus()` 订阅 user_state + weak_area
5. **2026-07-17**: TutorAgent 未接入 P0 → `handle()` 接受 `graph_context` 参数，`_handle_with_graph_context()` 使用图谱上下文
6. **2026-07-18**: `QuizRequest` 缺少 `user_id` → 添加 `user_id: Optional[str] = None`
7. **2026-07-18**: `_extract_topic_from_query` 主题提取失败 → 使用正则提取 `on/about/关于 {topic}` 和 `evaluate my {topic} level` 模式
8. **2026-07-18**: `resolve()` 抛异常 → 改为返回空列表 + PageRank Top-5 兜底
9. **2026-07-18**: 概念匹配过于严格 → `_match_fuzzy` 双向包含 + case-insensitive Python 回退（关键修复：Cypher 大小写敏感导致 "rag 技术" 不匹配 "RAG"，Python 回退逻辑成功匹配）
10. **2026-07-18**: `vector_store` 未传入 → Coordinator 传入 `HybridRetriever` 使 embedding 语义检索可用

**关键修复点**（第 9 轮）：
- `_match_fuzzy` 的 Cypher 查询是大小写敏感的：`c.name CONTAINS 'rag 技术'` 对 "RAG" 返回 False
- 新增 Python 回退逻辑：`'rag 技术'.lower() in 'RAG'.lower()` 或 `'RAG'.lower() in 'rag 技术'.lower()` → 匹配成功
- 这个 case-insensitive 回退是 P0 流程最终跑通的关键

**验证日志**（2026-07-18 01:27）：
```
[Coordinator] P0-INT-1: 使用图谱教育模块为 quiz 意图组装上下文
[Coordinator] 提取主题: rag 技术
[ConceptRetriever._match_fuzzy] Case-sensitive Cypher returned 0 nodes
[ConceptRetriever._match_fuzzy] Case-insensitive match: 'rag 技术' <-> 'RAG'
[ConceptRetriever.resolve] 策略2 成功: 模糊匹配到 'RAG' (共1个)
[Coordinator] 解析到 1 个种子概念
[Coordinator] 构建子图: 11 节点, 10 边
[Coordinator] 组装上下文: 910 tokens
[QuizAgent] P0-INT-2: 使用 P0 图谱上下文出题
[QuizAgent] 图谱上下文概念: ['RAG', 'RAG好处', 'RAG典型实现方法', ... '生成器模块']
```

**设计文档更新**：`docs/design-graph-education-agent.md` 新增"附录：P0-QUIZ 最终设计实现"章节，包含完整修复链路、最终架构图、关键接口定义、验证日志示例。

**相关文件修改**：
- `app/backend_api.py` (QuizRequest + EvaluateStartRequest 添加 user_id)
- `agents/coordinator.py` (主题提取正则 + vector_store 传入 + 消息总线)
- `agents/quiz_agent.py` (消息订阅 + 自适应出题)
- `agents/tutor_agent.py` (P0 接入 + graph_context 参数)
- `core/graph_education/concept_retriever.py` (双向匹配 + case-insensitive + 兜底策略 + 调试打印)

### LA-035-P27: 前端图片无法显示 ✅
- 状态：已解决（2026-07-18 验证通过）
- 修复：`GraphStore._sanitize_media_refs()` 使用相对路径 + 反斜杠替换
- 验证：刷新前端点击节点，NodeDetailPanel 正确显示图片

### LA-035-P28: 节点悬浮窗图片自适应 ✅
- 状态：已解决（2026-07-18 验证通过）
- 修复：去掉 `aspect-ratio` 强制比例，使用 `width/height: auto` + `max-width/max-height: 100%`
- 验证：悬浮在带图片节点上，图片完整显示、比例正确、不裁剪

### LA-035-P30: 文档树效果错乱 🔴 重新设计
- 状态：修复无效，延后处理
- 尝试：过滤 ADJACENT_TO 边 + 增大间距 + 缩短标签 → 用户反馈"和原先没变化"
- 计划：明天重新审查设计，可能需要换布局算法

## 新增遗留问题

### LA-040-P0-QUIZ-IMPROV: Top-N 种子概念选择（用户可选出题范围）
- 状态：新增改进点（P2）
- 描述：`resolve()` 当前只取 `nodes[0]`（Top-1），用户无法选择其他相关概念
- 期望：前端展示候选概念列表，用户可勾选，支持 Top-N 种子概念出题
- 方案：resolve 添加 `max_seeds` 参数，前端 Quiz 页面新增"出题范围"配置面板

## 今日 Git 提交

```
c954508 docs: P0-QUIZ resolved - 7 rounds of fixes, verified working
d6716c0 docs: update P0-QUIZ status with debug trace info
ada51d4 debug: add comprehensive tracing to ConceptRetriever
f0a230f fix: P0-QUIZ concept matching too strict — bidirectional fuzzy + vector_store + fallback
9f4ffd4 docs: update leftover-problem status - P28 resolved, P30 redesign, P0-QUIZ new root causes
1c9bc8a fix: P0-QUIZ topic extraction + ConceptRetriever no longer raises on no match
fa8a480 docs: update leftover-problem status - P27 resolved, P28/P30/P0-QUIZ re-fixed
4cbe807 fix: P0-QUIZ + P30 doc tree + P28 tooltip image
a450438 docs: update P0-QUIZ status - add TutorAgent P0 integration root cause
5644ba0 fix(agent): TutorAgent P0 integration - route concept intent through graph context
```

## 明日计划

1. **P30 文档树效果问题**：重新审查设计，可能需要换布局算法
2. **P29 删除学科残留**：如时间允许，测试并验证

---
*记录时间: 2026-07-18 01:35*