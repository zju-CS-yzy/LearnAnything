# LearnAnything 项目进展记录 - 2026-07-05

## 今日完成的工作

### 1. 确认有效的图谱构建方案（下午效果）
- **分树独立布局**：每棵树分别加载，独立运行 dagre 布局
- **树内节点去重**：同一棵树内多父节点不创建副本，由 dagre 原生处理 DAG
- **跨树副本**：多个上层节点指向同一个下层节点时，复制该下层节点到不同树中
- **垂直堆叠**：多棵树从上到下排列，间距 200px

### 2. 创建设计文档
- DesignDocuments/concept-view-layout.md - 概念视图布局设计文档
- 包含：数据结构、分树算法、布局原则、渲染设计、交互设计

### 3. 修复的关键问题
- **nextTick 导入**：Vue 3 的 
extTick 未导入导致加载中断
- **节点累积**：loadAllNodes() 未清除旧节点导致 1306+ 节点
- **数据一致性**：list_graph_concepts API 优先从 KùzuDB 读取（确保 ID 与边一致）
- **构建产物同步**：web/dist/ 与打包应用 dist/LearnAnything/_internal/web/dist/ 的同步问题

### 4. npm 环境修复
- 发现 package.json 的 dependencies 是空的，导致 
pm install 安装 0 个包
- 重新写入正确的 package.json
- 全局 vite 安装成功（
px --yes vite 可用）

## 遗留问题（前端完全失效）

### 问题 1：前端目录结构混乱
- web/ 目录经过多次修改、删除、重建，内部文件状态不一致
- web-vue/ 目录已被删除但之前是有效的构建目录
- web_bak/ 只包含部分备份，不是完整的可构建项目
- ront-end/ 目录创建后 npm install 仍然无法工作

### 问题 2：npm install 无法安装本地依赖
- 现象：
pm install 输出 "added 0 packages" 或安装全局包而非本地
- 已尝试：清除缓存、强制安装、verbose 模式、手动复制全局 node_modules
- 结果：本地 
ode_modules/.bin/vite 始终不存在

### 问题 3：构建产物无法正确部署
- 现象：后端返回 API JSON 而非前端页面
- 原因：INDEX_FILE = BASE_DIR / 'web' / 'dist' / 'index.html' 指向的目录不存在或为空
- 已尝试：复制 web/dist/ 到 dist/LearnAnything/_internal/web/dist/
- 结果：仍然显示 API JSON

### 问题 4：下午的有效代码已丢失
- 下午 15:00-16:00 的分树独立布局效果是最接近目标的
- 但该代码从未成功构建或备份到 Git
- 当前 web/src/ 下的代码是简化的单 dagre 布局，无分树逻辑

## 明日计划（2026-07-06）

### 第一步：创建干净的前端目录
新建 ront-end/ 目录，从头开始：
1. 正确初始化 package.json（包含 vite + vue 依赖）
2. 安装依赖（确保本地 node_modules 有 vite）
3. 创建最小可工作的 Vue 3 项目

### 第二步：恢复下午的分树布局效果
1. 基于设计文档实现分树独立布局
2. 实现树内副本策略
3. 实现垂直堆叠

### 第三步：Git 版本控制
每次修改后提交：
`ash
git add -A
git commit -m "描述修改"
`

### 第四步：验证构建和部署
1. 构建前端
2. 同步到打包应用
3. 验证后端能正确加载前端
4. 验证浏览器显示效果

## 关键文件位置
- 后端 API：pp/backend_api.py
- 设计文档：DesignDocuments/concept-view-layout.md
- 数据库：knowledge_base/graph_db/generic_v1_graph（37MB）
- 概念 CSV：knowledge_base/generic_v1_concepts.csv

## 注意事项
1. 每次构建后必须同步到 dist/LearnAnything/_internal/web/dist/
2. 浏览器必须强制刷新（Ctrl + Shift + R）
3. 后端必须重启才能识别新的静态文件
4. 数据库中 generic_v1 有 0 条语义边（需要重新构建或从备份恢复）
